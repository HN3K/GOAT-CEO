# Per-session GOAT pipeline artifacts (resume anchor, gates, status, logs)
agent-workspace/
logs/

# Python caches
__pycache__/
*.pyc
.pytest_cache/

# Local-only state — each user generates their own
repo-registry.json
.claude/agent-memory/

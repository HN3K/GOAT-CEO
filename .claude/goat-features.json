{
  "_comment": "SHIPPED baseline defaults for GOAT-CEO optional features. This file is committed and is the project's neutral, safe baseline — everything OFF. It is NOT a place for personal preferences: anything set here is shared with everyone who uses GOAT-CEO. PERSONAL default overrides belong in .claude/goat-features.local.json (gitignored, never published). Effective default = your local override if present, else this baseline. Precedence (most specific wins): built-in (off) < this shipped baseline < personal local defaults < per-repo override (repo-registry.json) < session sentinel (agent-workspace/).",
  "schemaVersion": 1,
  "defaults": {
    "rubric": "off",
    "research-kb": "off",
    "codebase-index": "off",
    "strict-mode": "off",
    "rubric-heal-gate": "off",
    "unattended": "off"
  },
  "_notes": {
    "rubric": "When on, a repo that is RUBRIC-AVAILABLE gets standards grounding + RUBRIC.GATE + Reviewer-C by default. Enablement still requires the repo to actually have a .rubric/ KB and the rubric CLI on PATH (detected at intake).",
    "research-kb": "When on, the technical researcher uses the shared research-kb/ (capture-always, verify-on-demand) instead of ephemeral WebSearch. Requires tools/research-system installed and research-kb/ present/creatable.",
    "codebase-index": "When on, agents prefer search/inject/check over direct reads where a Codebase-Index/ + tooling are present. Detection-driven per repo.",
    "strict-mode": "When on, documented degraded-allow paths (e.g. the test gate with no config) BLOCK instead of warn-allow. Also settable per-session via agent-workspace/STRICT_MODE or env GOAT_CEO_STRICT.",
    "rubric-heal-gate": "When on, RUBRIC-AVAILABLE target repos get the opt-in PostToolUse self-heal hook (rubric_heal_gate.py, capped <=2 cycles/file). Must be copied + wired into the TARGET repo's .claude/ (per-repo, manual).",
    "unattended": "When on, sessions default to the keep-going / survive-compaction layer (agent-workspace/AUTONOMOUS-ACTIVE). Leave OFF unless you genuinely run unattended; see commands/goat-ceo/unattended-mode.md."
  }
}
